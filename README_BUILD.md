# 🐾 Animal Auto Pilot - คู่มือการใช้งาน

## 📦 วิธีสร้างไฟล์ .exe

### ขั้นตอนที่ 1: ติดตั้ง cx_Freeze

เปิด Command Prompt หรือ PowerShell แล้วรันคำสั่ง:

```bash
python -m pip install cx_Freeze
```

หรือถ้าใช้ไม่ได้ ให้ลอง:

```bash
py -m pip install cx_Freeze
```

### ขั้นตอนที่ 2: Build ไฟล์ .exe

**วิธีที่ 1: ใช้ Batch File (แนะนำ)**

Double-click ที่ไฟล์ `build.bat` หรือรันใน Command Prompt:

```bash
build.bat
```

**วิธีที่ 2: รันคำสั่งเอง**

```bash
python setup.py build
```

### ขั้นตอนที่ 3: หาไฟล์ .exe

หลังจาก build เสร็จ ไฟล์ .exe จะอยู่ใน:

```
build/exe.win-amd64-3.x/Farm.exe
```

คุณสามารถคัดลอกโฟลเดอร์ `exe.win-amd64-3.x` ไปไว้ที่ไหนก็ได้ และรันไฟล์ .exe ได้เลย

---

## 🎨 วิธีใช้งาน UI

### เริ่มต้นใช้งาน

รันไฟล์:
```bash
python Farm_UI.py
```

หรือถ้า build เป็น .exe แล้ว ให้สร้าง `setup.py` สำหรับ UI:

```python
from cx_Freeze import setup, Executable

build_exe_options = {
    "packages": ["pyautogui", "time", "random", "tkinter"],
}

setup(
    name="AnimalAutoPilot_UI",
    version="1.0",
    description="Auto clicker with UI",
    options={"build_exe": build_exe_options},
    executables=[Executable("Farm_UI.py", base="Win32GUI")]  # ไม่แสดง console
)
```

แล้วรัน:
```bash
python setup.py build
```

### การใช้งาน UI

#### 1. เพิ่มตำแหน่งคลิก

**วิธีที่ 1: ใส่ตำแหน่งเอง**
- ใส่ค่า X และ Y
- กดปุ่ม "➕ เพิ่ม"

**วิธีที่ 2: ใช้เมาส์หาตำแหน่ง**
- กดปุ่ม "📍 Get Position"
- รอ 3 วินาที
- เลื่อนเมาส์ไปที่ตำแหน่งที่ต้องการ
- ตำแหน่งจะถูกใส่ในช่อง X, Y อัตโนมัติ
- กดปุ่ม "➕ เพิ่ม"

#### 2. ลบตำแหน่ง

- คลิกเลือกตำแหน่งในรายการ
- กดปุ่ม "🗑️ ลบ"

#### 3. ตั้งเวลารอ

- ใส่จำนวนนาทีที่ต้องการรอระหว่างรอบ (ค่าเริ่มต้น: 30 นาที)

#### 4. เริ่มทำงาน

- กดปุ่ม "▶️ เริ่มทำงาน"
- โปรแกรมจะรอ 5 วินาทีก่อนเริ่ม
- ดูสถานะได้ในช่อง "📋 สถานะ"

#### 5. หยุดทำงาน

- กดปุ่ม "⏹️ หยุด"
- หรือเอาเมาส์ไปมุมซ้ายบนของหน้าจอ (FAILSAFE)

---

## 🔧 Troubleshooting

### ปัญหา: pip ใช้ไม่ได้

**แก้ไข:**
```bash
python -m pip install cx_Freeze
```

หรือ:
```bash
py -m pip install cx_Freeze
```

### ปัญหา: Python ไม่อยู่ใน PATH

**แก้ไข:**
1. หา Python ที่ติดตั้งไว้ (เช่น `C:\Python39\python.exe`)
2. ใช้ path เต็มในการรันคำสั่ง:
```bash
C:\Python39\python.exe -m pip install cx_Freeze
C:\Python39\python.exe setup.py build
```

### ปัญหา: pyautogui ไม่ทำงาน

**แก้ไข:**
```bash
python -m pip install pyautogui pillow
```

### ปัญหา: Build แล้วไฟล์ .exe ไม่ทำงาน

**ตรวจสอบ:**
1. ต้องคัดลอกทั้งโฟลเดอร์ `exe.win-amd64-3.x` ไปด้วย (ไม่ใช่แค่ไฟล์ .exe)
2. ตรวจสอบว่ามี DLL files ครบ
3. ลองรันจาก Command Prompt เพื่อดู error message

---

## 📝 ไฟล์ที่สำคัญ

- `Farm.py` - โปรแกรมหลักแบบ command line
- `Farm_UI.py` - โปรแกรมแบบมี UI
- `setup.py` - ไฟล์สำหรับ build .exe
- `build.bat` - สคริปต์ build อัตโนมัติ
- `README_BUILD.md` - คู่มือนี้

---

## 💡 เคล็ดลับ

1. **ทดสอบก่อนใช้งานจริง**: ลองตั้งเวลารอ 1-2 นาทีก่อน
2. **ใช้ Get Position**: สะดวกกว่าการหาตำแหน่งเอง
3. **FAILSAFE**: จำไว้ว่าเอาเมาส์ไปมุมซ้ายบนเพื่อหยุดฉุกเฉิน
4. **ตรวจสอบ Log**: ดูสถานะการทำงานได้ในช่อง "📋 สถานะ"

---

## 🎯 ตัวอย่างการใช้งาน

### สำหรับเกม Farming

1. เปิดเกม
2. เปิด Farm_UI.py
3. ใช้ "📍 Get Position" เพื่อหาตำแหน่งที่ต้องการคลิก
4. เพิ่มตำแหน่งทั้งหมด
5. ตั้งเวลารอ (เช่น 30 นาที)
6. กด "▶️ เริ่มทำงาน"
7. โปรแกรมจะทำงานอัตโนมัติ

---

**สนุกกับการ Farming! 🐾**
